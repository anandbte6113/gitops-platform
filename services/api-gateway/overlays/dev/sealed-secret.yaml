# PLACEHOLDER — regenerate with: bash bootstrap/03-seal-secrets.sh
# This file must be sealed against the dev-cluster Sealed Secrets controller public key.
# Committing this placeholder so the kustomization.yaml reference is satisfied.
# Replace with real sealed secret before deploying.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: api-gateway-secret
  namespace: api-gateway
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  encryptedData:
    api-key: PLACEHOLDER_RUN_BOOTSTRAP_03_SEAL_SECRETS_SH
  template:
    metadata:
      name: api-gateway-secret
      namespace: api-gateway
